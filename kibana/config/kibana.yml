#
# ** THIS IS AN AUTO-GENERATED FILE **
#

# Default Kibana configuration for docker target
server:
  name: kibana
  host: 0.0.0.0
  ssl:
    enabled: true
    certificate: /usr/share/kibana/config/certs/kibana.crt
    key: /usr/share/kibana/config/certs/kibana.key
elasticsearch:
  ssl:
    certificateAuthorities: /usr/share/kibana/config/certs/ca/ca.crt
  hosts: 
    - "https://elasticsearch01:9200"
  username: "kibana_system"


##  instead of using `elasticsearch.password` entry
##  in this kibana.yml file, we use command:
##  `kibana-keystore add elasticsearch.password`
##  int the kibana environment,
##  and When prompted, enter the password for the 
##  kibana_system user. 
#elasticsearch.password: "password"

# kalau menggunakan image kibana versi oss, yg dibawah ini ga perlu
#xpack.monitoring.ui.container.elasticsearch.enabled: false

## Since environment variables are translated to CLI arguments, 
## they take precedence over settings configured in kibana.yml.